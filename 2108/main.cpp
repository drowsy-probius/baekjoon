#include <iostream>
#include <vector>
#define MAX 4000
using namespace std;

int main(void){
    ios::sync_with_stdio(false);
    vector<int> arr;
    int size=0;

    cin >> size;
    arr.resize(size);

    

    return 0;
}